A prrof-of-concept for TypeScript as an embedded language in haskell. Demonstrates:

- compatibility of TypeScript API with duktape (ES5)
- module loading in duktape
- calling back into haskell from JS

See `Main.hs`
